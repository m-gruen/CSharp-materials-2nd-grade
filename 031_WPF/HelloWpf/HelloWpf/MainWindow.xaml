<Window x:Class="HelloWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWpf"
        mc:Ignorable="d" Background="Aquamarine"
        Title="Hello WPF" Height="75" Width="250">
    <Viewbox>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="42" VerticalAlignment="Center" Margin="10 0 0 0" Name="NumberOne"/>
            <TextBlock Text="+" VerticalAlignment="Center" Name="Opperation"/>
            <TextBlock Text="10" VerticalAlignment="Center" Name="NumberTwo"/>
            <TextBlock Text="=" VerticalAlignment="Center"/>
            <TextBox Width="50" Margin="5" Background="WhiteSmoke" Name="ResultTextBox"
                     TextAlignment="Center" VerticalContentAlignment="Center" BorderBrush="Orange" BorderThickness="2"/>
            <Button Margin="0 5 5 5" Padding="10 0"
                    Click="OnCheck">Check</Button>
        </StackPanel>
    </Viewbox> 
</Window>
